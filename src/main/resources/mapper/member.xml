<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.home.member.mapper.MemberMapper">

	<insert id="signUp" parameterType="member">
		insert into member(id, pw, name, addr, tel) 
		values (#{id}, #{pw}, #{name}, #{addr}, #{tel})
	</insert>
	
	<select id="isMemberExists" parameterType="string" resultType="boolean">
    	select count(id) > 0 from member where id = #{id}
	</select>
	
	<select id="login" parameterType="member" resultType="int">
		select count(id) from member
		where id = #{id} and pw = #{pw}
	</select>
	
	<select id="searchMember" parameterType="string" resultType="member">
		select * from member
		where id = #{id}
	</select>
	
	<update id="updateMember" parameterType="member">
		update member set tel=#{tel}, pw=#{pw}, name=#{name}, addr=#{addr}
		where id = #{id}
	</update>
	
	<delete id="deleteMember" parameterType="string">
		delete from member
		where id = #{id}
	</delete>
	
	<select id="getMemberList" parameterType="string" resultType="member">
		select * from member
	</select>
	
</mapper>